# 期货看板日报 · 方法论与复制指南（v6 简化版）

> **一句话概括**：日报只看两个要素——**趋势**和**动量（score）**，各分**日线 / 4h** 两周期。
> 日线定方向，4h 定节奏；两周期 × 两要素交叉，多头侧分出四档：**绝对龙头 / 新贵 / 回调 / 危险分歧**，空头侧镜像。
>
> **版本说明**：v6（2026-09-16 重构）。相比 v5：九节模板压成五节；农产品/工业品分流、熊头遇压、排名雷达、转折 A/B/C、EE 生死线清单、回踩 vs 分歧决策树**全部删除**，由四档分类统一替代。**增减仓**是第三要素，但数据源暂未接入，仅作预留（接入后：日线增仓 = 对当侧趋势的增强确认）。
>
> **🎯 锚点样板**：**`daily_report_2026-09-22_v6.md` / `.html`（v6.2，2026-09-22 阿成钦定）** —— 以后每期出刊照抄这份的结构与视觉，或在其上迭代，不要另起炉灶、不要回退旧结构。（阿成原话：「以最新的这个 V6.2 日报为榜样。以后的日报格式和方法论以此为锚点。」）
>
> **演进**：v6.1（09-22）新贵档加 4h 前置 → v6.1+ 该原理升格为基本原则 3 → **v6.2（09-22）新增 🟢 4h 蓄势池**，本节即为锚点版。

---

## 目录

- [0. 先看成品](#0-先看成品)
- [1. 框架：两要素 × 两周期](#1-框架两要素--两周期)
- [2. 四档定义（多头侧 · 空头侧镜像）](#2-四档定义多头侧--空头侧镜像)
- [3. 报告格式：五节固定模板](#3-报告格式五节固定模板)
- [4. 执行流程](#4-执行流程)
- [5. 数据字段速查](#5-数据字段速查)

---

## 0. 先看成品

> **🎯 出刊前先打开锚点样板**：`daily_report_2026-09-22_v6.md` / `.html`（v6.2）。结构、表格列、卡片栅格、颜色类、提示块位置全部照抄，**只换数据不换骨架**。

以 **2026-09-16 版**（数据基准 09-15 收盘）为例，四档分完后核心结论：

| 档位 | 品种 |
|---|---|
| 🥇 绝对龙头（7 只） | eg / bu / fu / PR / PF / PX / lu —— 全在能源链＋聚酯链 |
| 🟡 回调（6 只） | sc（日线 20.80 但 4h 仅 +0.32，不入龙头档）/ pg / MA / bz / eb / PL |
| 🔴 危险分歧（4 只） | bc（日线转负＋4h 出榜）/ pp / l / ec（4h 破位） |
| 🐻 绝对熊头 | LC 碳酸锂（双周期双空）；CJ 红枣（日线 −14.49） |
| 🔴 空头侧异动 | c 玉米 / pb 铅 当日新开空，开空即在 KK 下方 |

交付物：`.md` + `.html`（浅色卡片版）。文件命名 = **下一交易日**。

---

## 1. 框架：两要素 × 两周期

| | **日线（1d）** | **4小时（4h）** |
|---|---|---|
| **趋势** | 大方向：POS=1 持多 / −1 持空 / 0 离场 | 节奏：是否在多头桶（`long_trend`） |
| **动量** | `score`：正=多，负=空，绝对值=强度 | `score`：同左，看**环比变化 Δ4h** |

**四条基本原则**

1. **日线定趋势，4h 定节奏** —— 4h 的一切变化以日线方向为最终裁决。
2. **动量看数值也看变化** —— 日线 score 看绝对水平；4h score 重点看**环比 Δ4h = 今日 − 前一交易日**。
3. **环比必须站在绝对水平之上才有意义** —— Δ4h 的改善/恶化，只有在 4h score 本身越过阈值（多 `+1.0` / 空 `−1.0`）后才作数。**负值区里的环比大幅改善 = 空头力竭，不是多头进攻**，不入新贵档。（这条正是新贵档 4h 前置条件的原理，见 §2 注）
4. **分档互斥，按优先级取档** —— 一只品种一天只属于一个档。

**阈值表（可调）**

| 参数 | 默认值 | 含义 |
|---|---|---|
| `LEAD_1D` | 4.5 | 日线动量高门槛 |
| `LEAD_4H` | 1.0 | 4h 动量强门槛 |
| `D4H_SIG` | 1.0 | 4h 动量环比显著变化门槛（\|Δ4h\| ≥ 此值） |
| `NEW_STRONG_4H` | 1.0 | **新贵档前置：4h score 必须 > 此值**（2026-09-22 增补，见 §2 注） |

---

## 2. 四档定义（多头侧 · 空头侧镜像）

### 多头侧

按以下顺序判定，**满足即定档，不再往下走**：

| 优先级 | 档位 | 定义 | 动作 |
|---|---|---|---|
| 1 | 🥇 **绝对龙头** | 日线多 ＋ 1d score ≥ 4.5 ＋ 4h 在多头桶 ＋ 4h score ≥ 1.0 | 拿住，核心仓 |
| 2 | 🔴 **危险分歧** | 日线多 ＋ 4h 偏弱（掉出多头桶或 4h score < 0）＋ Δ4h ≤ −1.0 | 减仓/撤 |
| 3 | 🚀 **新贵** | 日线多 ＋ Δ4h ≥ +1.0 ＋ **4h score > 1.0**（未达龙头档） | 新主线候选，关注/试多 |
| 4 | 🟡 **回调** | 日线多 ＋ 1d score ≥ 4.5 ＋ Δ4h > −1.0 | 持有，不砍 |
| — | ⚪ 未入档 | 日线多但不满足以上任一条（如 1d < 4.5 且 Δ4h 平缓） | 表格可省略，**但 4h 强的要进「蓄势池」**（见下） |

> ⚠️ **⚪ 未入档不是「没信号」，要分流**：四档里龙头/回调都要 `1d ≥ 4.5`、新贵要 `Δ4h ≥ +1.0`，于是**「4h 已经很强、日线还没跟上」的品种会掉进 ⚪ 空档位**。这类必须转入 **🟢 4h 蓄势池**（见 §3 附表），不能一句「未入档」带过 —— 它们是明日最可能入档的名单。分流标准：**4h 在多头趋势桶 ＋ 4h score > `NEW_STRONG_4H`**。反例：pb 铅 4h +0.55、AP 苹果 4h +0.36 → 水平不足，仍算真·未入档。

> **🚀新贵必须 4h 也强（2026-09-22 修订）**：新贵的定义是「**新主线启动**」，不是「跌后回抽」。
> 若只要求 Δ4h ≥ +1.0，则「4h 已掉桶、收破 4h EE、但 score 环比回升」的品种也会被标成新贵 ——
> 语义完全相反（一个是要买的新势力，一个是该减的破位品种）。
> 故增补前置条件 **4h score > 1.0**（与多头强门槛 LEAD_4H 同值）：
>
> - **Δ4h ≥ +1.0 且 4h > 1.0** → ✅ 🚀 新贵（4h 真的转为强势）
> - **Δ4h ≥ +1.0 但 4h ≤ 1.0** → ❌ 降级：1d ≥ 4.5 走 🟡 回调；1d < 4.5 走 ⚪ 未入档
>   （若同时还掉桶，会被优先级 2 的 🔴 危险分歧先截走）
>
> 实例（09-21 数据）：fu（4h −0.86）、pg（−1.16）原被标 🚀 新贵 → 修正后降为 🟡 回调；
> sp（4h +2.43）保留 🚀 新贵。

> 注意与 v5 的差别：**「日线超强但 4h 只是弱正」不再单设"绝对龙头"特殊档**（如 sc 原油 1d 20.80 / 4h +0.32 → 归回调档）。龙头必须双周期双强，没有例外。

> **关于新贵档的 4h 前置条件（v6.1，2026-09-22）**：新贵档要求 `Δ4h ≥ +1.0` **且** `4h score > 1.0`。原因是基础原则 3 —— **环比是「变化」，绝对值是「水平」；变化只有站在一定水平之上才有意义**。
> - 4h 已在 **+1.0 以上**、Δ4h 再上 +1.0 → 高位加速，**真·新主线启动** ✅ 入新贵档。
> - 4h 还在 **负值区**（如 −0.9 → −0.1）、Δ4h 同样 +1.0 → 只是**力竭式回抽**（空头打不动了，不等于多头进攻）❌ 不入新贵档，按 1d 是否 ≥ 4.5 降为 🟡 回调 / ⚪ 未入档。
> - 同理，空头侧的 📉 空头新贵要求 `4h score < −1.0` —— 正区里的环比恶化只是多头接力乏力，不是空头进攻。
> - 实例（09-21 收盘）：fu 4h −0.86 / Δ4h +2.35、pg 4h −1.16 / Δ4h +1.76 均属负区回抽，不入新贵；sp 4h +2.43 / Δ4h +1.32 才是真新贵。

### 空头侧（镜像，score 符号取反）

| 优先级 | 档位 | 定义 | 动作 |
|---|---|---|---|
| 1 | 🐻 **绝对熊头** | 日线空 ＋ 1d score ≤ −4.5 ＋ 4h 在空头桶 ＋ 4h score ≤ −1.0 | 空单拿住 |
| 2 | 🔴 **空头危险分歧** | 日线空 ＋ 4h 转强（掉出空头桶或 4h score > 0）＋ Δ4h ≥ +1.0 | 空单减/撤，防反转 |
| 3 | 📉 **空头新贵** | 日线空 ＋ Δ4h ≤ −1.0 ＋ **4h score < −1.0**（未达熊头档） | 新空主线候选，可跟空 |
| 4 | 🟡 **反抽** | 日线空 ＋ 1d score ≤ −4.5 ＋ Δ4h < +1.0 | 空单持有，别抄底 |
| — | ⚪ 未入档 | 日线空但不满足以上任一条 | 4h 在空头桶且 4h < −1.0 的进「空头蓄势池」（镜像） |

### 🟢 4h 蓄势池（跨档补充 · 2026-09-22 新增）

**定义**：`4h 在多头趋势桶` ＋ `4h score > 1.0` ＋ `未进四档实档（即判为 ⚪）`。空头侧镜像：`4h 在空头趋势桶` ＋ `4h score < −1.0`。

**它解决什么**：四档的门槛全在 1d 这一侧（≥4.5）或 Δ 这一侧（≥+1.0），于是**4h 本身已经很强、只是日线还没跟上的品种，会被判 ⚪ 后从报告里消失**。蓄势池就是把这个盲区捞回来。

**为什么叫「蓄势」不叫「新贵」**：按原则 3，4h 水平已经够（站上 1.0），但**日线趋势这一侧还没确认** —— 差的是日线 BK 或 score 抬升，所以是蓄势，不是启动。

**动作**：**小仓试多 / 观察**，等日线 `score ≥ 4.5` 或日线 BK 确认后提级进实档；4h 掉出多头桶即出池。

**与四档的关系**：**不冲突**，蓄势池是四档之外的补充标记，一只品种可以「四档里是 ⚪ ＋ 同时挂在蓄势池」。报告里单列一小节（紧跟多头四档表之后）。

**实例（09-21 收盘）**：588000 科创50ETF（4h 3.87）/ 000852 中证1000（4h 2.72）/ IM 中证1000·09（4h 1.93）三只入池，日线 score 分别仅 0.81 / 0.94 / 1.47；同期的 pb 铅（4h 0.55）、AP 苹果（4h 0.36）水平不足，仍算真·未入档。

### 4h 状态备注（只留最简口径）

- **在桶 / 掉出（平仓）**：看 `bucket` 是否仍在 `long_trend` / `short_trend`。
- **破位**：4h 收盘跌破 4h EE（多头侧），在备注栏标「破位」一词即可，不单独设档。
- **「4h score 转正/转负」≠「重新入桶」**：结构变化只看桶归属，不看分数符号。

---

## 3. 报告格式：五节固定模板

模板骨架：`TEMPLATE_daily_report_v6.md`（复制它开始写，每期只填数不改框架）。

**头部**：数据基准 / 对比基准（多空计数一行）/ 看板链接 / 一句话定性。

| # | 章节 | 回答的问题 | 写法要点 |
|---|---|---|---|
| 一 | 核心判断 | 今天最重要的 3~5 件事 | 每条 = 编号 + emoji 定性；事实数字 → 定性 → 动作 |
| 二 | 多头阵营 · 四档表 | 多头谁在领、谁在歇、谁危险 | 固定列：**品种 ｜ 日线 score ｜ 4h score ｜ Δ4h ｜ 排名轨迹(7日) ｜ 档位 ｜ 一句话** |
| 三 | 空头阵营 · 镜像四档表 | 空头同上 | 列同二 |
| 三·补 | 🟢 **4h 蓄势池** | 4h 已强但日线未达门槛的是谁 | 紧跟表三之后，一小段；品种 ｜ 4h ｜ Δ4h ｜ 日线 score ｜ 入池理由 ｜ 提级条件。无品种则写「本期无」。 |
| 四 | 动量异动榜 | 谁在加速、谁在失速 | Δ4h 环比变化最大的品种，多/空方向各取前 ~5，一行一只 |
| 五 | 操作提示 | 明天开盘怎么干 | ≤8 条，每条一个动作，倒金字塔 |
| 尾 | 口径说明 | 术语与阈值 | 固定文案：四档定义 + 阈值 + Δ4h 口径 + 增仓预留说明 |

**排版三条**：倒金字塔（结论在前）；一只品种只给一个档位标记；数字打头（写"6/7 已倒"不写"大部分"）。

### 3.1 锚点样本骨架（v6.2 · 照抄用）

> **钦定样板**：`daily_report_2026-09-22_v6.md` / `.html`。以下是它的完整骨架，出刊时**逐节对齐**。

| # | 段落 | 必含要素 |
|---|---|---|
| 头 | 元信息 | 4 行固定（数据基准 / 对比基准 / 看板 / 本次运行）＋ **版本行**（写清当期版本号与本期生效的规则变更）＋ 金色一句话定性 |
| 一 | 核心判断 | 开头 `0️⃣ 当日信号动作（必读）` 块：1d 与 4h 的 BK/SP/SK/BP **全列不省略**；其后 3~5 张 `judge-card`，每张 = 编号+emoji 标题 / **事实**（带数字）/ **动作**（结论加粗） |
| 二 | 多头四档表 | 7 列；按档位优先级排序；表下一句汇总「未入档 X 只」并**点名需转蓄势池的**；紧跟 `.note` 特别警示（如 m 豆粕类贴生死线品种） |
| 三 | 空头镜像四档表 | 同二；表下 note 记「空头榜排名上升 = 塌陷假象」类提醒 |
| 三·补 | 🟢 蓄势池 | 6 列（品种 / 4h / Δ4h / 日线 score / 入池理由 / 提级条件）；表下 note 说明同群特征与**未入池的真·未入档品种**；本期无则写「本期无」 |
| 四 | 动量异动榜 | 多向加速（前 5~8）/ 空向失速（前 5）各一条一行，**每条必须与档位交叉印证**；末尾「排名异动（\|Δrank\|≥3）」一段 |
| 五 | 操作提示 | ≤8 条，倒金字塔；每条 = 名单 + 动作；最后一条固定为「明日复核重点」编号清单 |
| 尾 | 口径说明 | 固定段：四档定义（含新贵 4h 前置）＋ 🟢 蓄势池定义 ＋ Δ4h 口径 ＋ 塌陷假象 ＋ 一只一档 ＋ 增仓预留 ＋ 数据源 |

**HTML 视觉类固定**：`.header`（深色）/ `.oneline`（金色一句话）/ `.card` / `.judge-grid`+`.judge-card`（`.j-red` `.j-gold` `.j-green` 左边框）/ `.note`（黄色提示块）/ 表格 / `.lead .pull .danger .bear .fresh .flat`（六色语义类）/ `.foot`。**只改内容，不改样式类名。**

**排版三条**：倒金字塔（结论在前）；一只品种只给一个档位标记；数字打头（写"6/7 已倒"不写"大部分"）。

---

## 4. 执行流程

1. **抓数据**：拉 `/api/screening`、`/api/screening?timeframe=4h`、`/api/symbols`、`/api/symbols?timeframe=4h`，归档 `data/{screen,symbols}_{1d,4h}_<YYYYMMDD>.json`。先看 `generated_at` 确认是今天收盘（1d 约 16:20 后，4h 约 15:36 后）。
2. **算 Δ4h**：读**前一交易日**归档的 4h 数据，逐品种算 `Δ4h = score_今日 − score_前日`。前日归档缺失时，Δ 栏标「—」，分档按当日 4h 状态**暂定并加 ※**。
3. **分档**：按第 2 节优先级逐品种归四档。过滤 `close is None` 的旧合约；品种码 = 合约码剥字母；POS/last_signal 从 symbols 接口取。
   **再跑一遍蓄势池分流**：判为 ⚪ 的品种里，`4h 在多头桶 ＋ 4h > 1.0` 的捞进 🟢 蓄势池单列，不许一句「未入档」带过。
4. **填模板**：复制 `TEMPLATE_daily_report_v6.md` → `daily_report_<下一交易日>.md`；HTML **优先复制锚点样板 `daily_report_2026-09-22_v6.html`**（骨架最完整），只改内容与样式以外的一切不动。
5. **校验**：HTML 跑开闭标签计数（`table/tr/td/div/h2/ul/li` 开闭数相等），不配对则手机上必乱。
6. **归档**：报告 + 数据 + 素材按日期存。

---

## 5. 数据字段速查

| 字段 | 含义 | 用法 |
|---|---|---|
| `score` | 强度分（正多负空） | 分档门槛、排序 |
| `close` | 最新收盘价 | **过滤 `None` 合约**；破位判断 |
| `DD / EE` | 多头支撑 / 生死线 | 破位 = 收破 EE（仅作备注用词） |
| `KK / PP` | 空头压力 / 止损线 | 空头侧备注 |
| `pos` | 持仓方向（1/−1/0） | 判日线趋势 |
| `bucket` | 所属桶 | **判 4h 在不在桶的唯一依据** |
| `last_signal` | 最近交易信号（BK/SP/SK/BP + 日期） | 信号新鲜度 |
| `rank / rank_change / rank_history` | 榜内名次 / 变化 / 7 日轨迹 | 表格「排名轨迹」列 |
| `generated_at` | 数据生成时间 | 确认数据已更新 |
| `bar_color` | 最后一根 K 线颜色 | ⚠️ 非趋势色，禁止用于分档 |

**Δ4h 口径**：`Δ4h = 4h score(今日收盘) − 4h score(前一交易日收盘)`，依赖两期归档文件，缺一不可。

**增仓预留**：第三要素「增减仓」暂未接入。接入后规则：日线增仓 = 对当侧趋势的增强确认（多头侧增仓加分、空头侧增仓加分），只作加减分项，不改变四档定义。

---

*v6（2026-09-16）：从 v5 的规则修补路线回到源头——趋势 × 动量 × 两周期，四档互斥，镜像对称。*
*v6.1（2026-09-22）：🚀新贵 / 📉空头新贵 增补 4h 强势前置（4h score > 1.0 / < −1.0），杜绝「破桶回抽」被误标为新贵。*
*v6.2（2026-09-22）：新增 🟢 4h 蓄势池，捞回「4h 已强、日线未达门槛」被判 ⚪ 后消失的品种。*
